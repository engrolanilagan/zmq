#
# For a description of the syntax of this configuration file,
# see the file kconfig-language.txt in the NuttX tools repository.
#

config ZMQ_HWCLIENT
	tristate "\"ZMQ HelloWorld Client\" example"
	default n
	---help---
		Enable the ZMQ TCP Client example

if ZMQ_HWCLIENT
endif
