#
# For a description of the syntax of this configuration file,
# see the file kconfig-language.txt in the NuttX tools repository.
#

config ZMQ_HWSERVER
	tristate "\"ZMQ HelloWorld Server\" example"
	default n
	---help---
		Enable the ZMQ TCP Server example

if ZMQ_HWSERVER
endif
